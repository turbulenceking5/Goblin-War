# Goblin War

A browser-based overworld RPG. No build step, no framework — every page is a single self-contained `.html` file with inline `<style>` and `<script>`, opened directly or served as static files. Live gameplay state lives in `localStorage` while playing, shared across pages by convention (same key names, no formal schema) — but the durable copy is a row in Supabase (hosted Postgres + auth), talked to directly over its JS client loaded from a CDN `<script>` tag (no server code of ours, just a public anon key and Row Level Security). **Every player page requires a logged-in account with an active character** (see [context/accounts.md](context/accounts.md) and [context/characters.md](context/characters.md)) — an account can own a roster of characters, picked at login, one save per character, autosaved as you play.

Fictional setting only — "Goblin War" and its world data have no connection to any real organization, event, or person.

## Files at a glance

| File | Role |
|---|---|
| [index.html](index.html) | The overworld map — pan/zoom, travel, camp, combat. This is the game's hub; every other page links back to it. |
| [character.html](character.html) | Hero sheet (HP/stamina/gold/food/weight/age/location) — mostly read-only, but Equipment can be unequipped from here. |
| [inventory.html](inventory.html) | Bag contents, gold, and carried weight — and where equippable items get equipped (see [context/player-state.md](context/player-state.md)). |
| [party.html](party.html) | Stub — no companion system yet. |
| [settings.html](settings.html) | The active character's current status, Save Now / Switch Character / Log Out, a Change Log menu item linking out to changelog.html, and Delete This Character. No local save slots anymore — see [context/characters.md](context/characters.md). |
| [changelog.html](changelog.html) | The full `CHANGELOG` history — split out of settings.html into its own page so Settings itself stays short. Marks the viewing account's changelog-seen key as caught up, same as settings.html used to. |
| [login.html](login.html) | Sign up / log in (email + password via Supabase Auth). Every other player page redirects here if there's no valid session. Success always routes to characters.html next (carrying along `?redirect=`, the page the player actually wanted), never straight into the game — see [context/accounts.md](context/accounts.md). |
| [characters.html](characters.html) | Choose, create, or delete a character. Every gated page redirects here (after login.html, if needed) whenever no active character is selected. See [context/characters.md](context/characters.md). |
| [game-map.html](game-map.html) | Legacy prototype map (scroll-based, pre-dates index.html's pan/zoom canvas). Not linked from anywhere — dead code, kept for reference only, and the only player-facing page **not** behind the login gate (nothing links to it, so nothing needs to). |
| [version.json](version.json) | `{ changelogId }` — the latest `CHANGELOG` entry's id. index.html polls this (cache-busted, every 5 minutes and whenever the tab regains focus) and shows an "Update available" banner if it's ahead of the id baked into the already-loaded page. Bump it alongside a new `CHANGELOG` entry (see the Conventions bullet below) or returning players on an old tab never see the banner. |
| [manifest.json](manifest.json) | Web App Manifest — `display:standalone`, `start_url:index.html`, icons. Every page links it (`<link rel="manifest">`) plus an `apple-touch-icon`, so "Add to Home Screen" installs a real app icon and stays in standalone mode (no browser chrome) across page navigations rather than reopening in Safari/Chrome. Re-adding the home-screen icon is required after any manifest/icon change — it's only read at install time. |
| `assets/icons/` | `icon-192.png` / `icon-512.png` (manifest) and `icon-180.png` (`apple-touch-icon`) — resized from `DisplayPicture.jpeg` (the source art, kept for regenerating at other sizes later). Re-adding the home-screen icon is required after changing these — see [manifest.json](manifest.json)'s row above. `assets/icons/Map/Cities.png` is unrelated — the City/Capital marker drawn on the game map itself (see [context/travel-and-map.md](context/travel-and-map.md)), not an app icon. |
| `assets/enemies/` | Real portrait art for combat (`#combat-enemy-icon`), one PNG per enemy variant (e.g. `goblin-raider.png`) — transparent background, downscaled to a 600px longest edge. All three races (Bandit, Goblin, Ork) have real art now. See [context/combat.md](context/combat.md)'s `ENEMY_VARIANTS`. |
| `assets/travel-graph.json` | Processed world data the game actually reads: settlements + road/sea graph. |
| `assets/world-raster.jpg` | The map image rendered under the SVG overlay. |
| `assets/game-map.json` | Raw Azgaar Fantasy Map Generator export `travel-graph.json` was built from. Not fetched by any page at runtime. |
| `assets/supabase-config.js` | The project URL + public anon key for the Supabase client. Not a secret. Loaded first, synchronously, in every gated page's `<head>`. |
| `assets/auth-gate-sync.js` | The synchronous, no-network first pass of the login + character gate — loaded right after supabase-config.js in every gated page's `<head>`, before any body content. See [context/accounts.md](context/accounts.md). |
| `assets/auth-client.js` | The authoritative (async) half of the login + character gate — creates the shared `sb` client and `authGateReady` promise every page's own script uses. Loaded near the bottom of every gated page, after the Supabase JS CDN script. See [context/accounts.md](context/accounts.md). |
| `supabase/schema.sql` | The `characters` table + Row Level Security policies (one row per character, not per account) backing the whole save system. Not run automatically — paste it into the Supabase dashboard's SQL Editor once per project. Kept here so the schema is versioned. |

## Context files

Each covers one system in depth — read the relevant one before changing that part of the game:

- [context/travel-and-map.md](context/travel-and-map.md) — the map canvas, road-graph pathfinding, travel animation, camp/stop-mid-journey, calendar.
- [context/player-state.md](context/player-state.md) — health/stamina/food/weight/gold/age/inventory: the `localStorage` keys every page shares.
- [context/combat.md](context/combat.md) — the turn-based bandit encounter system and how it's triggered.
- [context/locations-and-camp.md](context/locations-and-camp.md) — the settlement location-view (Inn, Marketplace, etc.) and the camp screen.
- [context/factions-and-territory.md](context/factions-and-territory.md) — the weekly faction AI tick, settlement conquest, and war-driven Marketplace pricing.
- [context/accounts.md](context/accounts.md) — the mandatory login gate, login.html, and the shared `assets/auth-gate-sync.js`/`assets/auth-client.js` scripts.
- [context/characters.md](context/characters.md) — one save per character, chosen at login: the `characters` table, characters.html, autosave in index.html, and Settings' Save Now/Switch Character/Delete This Character.
- [context/secondary-pages.md](context/secondary-pages.md) — character.html, inventory.html, party.html.
- [context/data-files.md](context/data-files.md) — the shape of `travel-graph.json` and where the map assets come from.
- [context/roadmap.md](context/roadmap.md) — every "coming soon" stub in the codebase, collected in one place.

## Conventions worth knowing before editing

- **No modules, no bundler.** Each HTML file's `<script>` is copy-pasted, self-contained JS — shared logic (e.g. the calendar formatter, the `localStorage` key names) is duplicated across files rather than imported. When you change a constant or a formula in one page, check the others for the same duplicated logic. The one deliberate exception is the login gate (`assets/auth-gate-sync.js` + `assets/auth-client.js`, see [context/accounts.md](context/accounts.md)) — genuinely shared, not copy-pasted, because it's security-relevant control flow rather than game logic: one page's copy silently drifting from another's would be a login bypass, not just a display bug.
- **Shipping a change? Add a `CHANGELOG` entry — in three places.** The `CHANGELOG` array (id/date/changes) is duplicated identically in index.html (drives the "What's New" popup shown on map load once the viewing account's `goblinwar_lastSeenChangelog_<user id>` falls behind the newest id — see [context/accounts.md](context/accounts.md)) and changelog.html (renders the full history, and bumps that same key so visiting it doesn't also trigger the popup). Add one new `{id, date, changes}` entry — id one higher than the last — to both arrays whenever a player-visible change ships, or the two pages will disagree about what's new. Also bump `version.json`'s `changelogId` to match — that's what index.html's "Update available" banner polls to know a newer build exists on the server; forgetting it means the banner never fires for that release.
- **Dark theme, kept consistent across pages.** Every page (except the unused game-map.html) uses the same dark-parchment palette: page/card backgrounds are near-black warm browns (`#1c150c` page bg, `#2b2115`→`#1c150c` gradients for raised cards/sheets/modals), body text is `var(--parchment)` (`#e9dcb8`), muted/label text is `#a9926a`, and card borders are a soft translucent gold (`rgba(209,164,69,.35)`, `.4` on inputs/buttons) rather than the old solid `var(--parchment-shadow)`. `var(--gold)`/`var(--gold-lt)` stay the same in both — they already read well on dark. Match these tokens when adding new UI rather than reintroducing a light card.
- **`localStorage` holds the live game; Supabase holds the durable save.** While playing, every page reads/writes the same `goblinwar_`-prefixed `localStorage` keys (e.g. `goblinwar_health`) as before — see [context/player-state.md](context/player-state.md) for the full list. But that's no longer the only copy: the active character's row in Supabase's `characters` table is autosaved from it periodically (see [context/characters.md](context/characters.md)), and is what actually survives across devices, browsers, or a cleared cache. Don't add a new persisted key to `localStorage` alone and assume it's safe — if it should survive a save, it needs to be added to the snapshot shape characters.md describes too.
- **Every internal navigation cache-busts with `?_=' + Date.now()`.** The game is deployed as static files behind a CDN with real HTTP caching (GitHub Pages: `Cache-Control: max-age=600`), and installed home-screen PWAs can hold onto stale pages even longer than that. Without a cache-buster, tapping a nav button or "Back to Map" shortly after a new version ships can silently reopen the *previous* version of that page — this actually happened (Settings kept showing the old inline Change Log after an update). So every same-app link — nav icons, "Back to X" buttons, post-action redirects (`location.href`/`location.replace`) — appends a timestamp query param, forcing a fresh fetch instead of a cache hit. `index.html`'s own "Update available" banner reload already did this; it's now consistent everywhere. A new nav link needs the same treatment.
- **Map coordinates are dual-scale.** World data (`travel-graph.json`, SVG overlay) uses a 2560×1277 "logical" space; the raster image is 10240×5108 (4× that). `STAGE_W/LOGICAL_W` converts between them — see [context/travel-and-map.md](context/travel-and-map.md).
- **Tone/flavor text is Chatty English fantasy** (rumours, ambush messages, location descriptions) — match that voice when adding more.
- **Full-screen overlays are z-index layered, and it's easy to get wrong.** index.html stacks `#location-view` (60), `#combat-view` (70), and `#toast` (80, deliberately above both) on top of the normal map UI. `showToast()` can fire from inside either overlay (Work, Inn, Marketplace, combat results), so the toast must outrank every overlay it can be called from, not just the base map chrome — it was briefly rendering invisibly behind `#location-view` for exactly this reason. If you add a new full-screen overlay, either give it a z-index below 80 or bump the toast again.
- **`env(safe-area-inset-*)` matters on installed iOS home-screen apps.** `viewport-fit=cover` (every page's viewport meta) lets content draw under the notch/status bar and home indicator; every sticky/fixed element pinned to a true screen edge — headers, `#toast`, `#travel-controls`, `#recenter-btn`, `#camp-here-btn`, `#info-card`, `#loc-actions`, `#combat-header` — adds the relevant `env(safe-area-inset-top/bottom)` into its own padding/position so it doesn't get clipped there. A new fixed-position element needs the same treatment; it's easy to miss on a new full-screen overlay specifically, since those don't inherit the normal `<header>`'s padding — `#loc-actions`'s "Back to Map" button and `#combat-header`'s title were both found clipped under the notch after being added without it.
